language: cpp

compiler:
  - gcc

before_install:
  - sudo apt-get update
  - sudo apt-get install -y build-essential yasm git libavcodec-dev libswscale-dev libass-dev python3-setuptools 
  python3-dev
  - sudo easy_install3 pip
  - pip3 install cython
  - git clone https://github.com/vapoursynth/vapoursynth.git

before_script:
  - chmod +x configure

script: ./configure

branches:
  only:
    - master

notifications:
  email: false

os:
  - linux
